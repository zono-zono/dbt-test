snapshots:
  - name: orders_scustomers_snapshotnapshot
    relation: ref('stg_customers')
    config:
      schema: snapshots
      unique_key: customer_id
      strategy: check
      check_cols: 'all'
